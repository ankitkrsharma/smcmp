server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smcmp
    username: root
    password: ankit123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  security:
    user:
      name: disabled
      password: disabled

logging:
  level:
    org.hibernate.SQL: DEBUG
